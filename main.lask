install_path = "$HOME/.local/bin"
test() = $ stack test
doctest() = $ stack exec doctest app/** src/**
install() = $ stack --local-bin-path #{install_path} install
uninstall() = $ rm #{install_path}/lask
